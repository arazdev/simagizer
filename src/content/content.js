// Content script for Simagizer
// Runs on web pages to assist with text extraction

// This script is injected into web pages but most extraction
// is done via chrome.scripting.executeScript from the popup
console.log('Simagizer content script loaded');
